@using MvcPaging
@model MvcPaging.IPagedList<lifu.Models.Service>

@{
    ViewBag.Title = "客戶服務";
}

<h2>客戶服務</h2>

<p style="display: none">
    @Html.ActionLink("新增", "Create")
</p>

    @using (Html.BeginForm())
{
<div>
                @Html.DisplayNameFor(model => model.FirstOrDefault().Name):<input id="Name" name="Name" type="search" value="@ViewBag.Name" />
            </div>
 <div>
        <input type="submit" value="搜尋" />
    </div>

    
}

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().TelphoneAM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().TelphonePM)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Mobile)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().ShortMemo)
        </th>
       
        <th>
            @Html.DisplayNameFor(model => model.FirstOrDefault().InitDate)
        </th>
        
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TelphoneAM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.TelphonePM)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mobile)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ShortMemo)
        </td>
     
        <td>
            @Html.DisplayFor(modelItem => item.InitDate)
        </td>
       
        <td>
           @* @Html.ActionLink("編輯", "Edit", new { id=item.Id }) |*@
            @Html.ActionLink("內容", "Edit", new { id = item.Id }) | 
            @Html.ActionLink("刪除", "Delete", new { id=item.Id })
            @Html.Hidden("ID",item.Id)
        </td>
    </tr>
}

</table>

<div class="pager">
    @Html.Pager(Model.PageSize, Model.PageNumber, Model.TotalItemCount)
</div>

